export default function DemoVidPage() {
  return (
    <div className="flex items-center justify-center min-h-screen bg-black text-white text-sm font-mono">
      Demo video lives in sayso_demo_video project. Run it separately at localhost:3000.
    </div>
  );
}
